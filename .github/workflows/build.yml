name: Build

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '11'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build jars
        run: ./gradlew --no-daemon jar shadowJar

      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: gps-bookmark-plugin
          path: |
            build/libs/*.jar
            !build/libs/*-all.jar
          if-no-files-found: error

      - name: Upload shadow jar
        uses: actions/upload-artifact@v4
        with:
          name: gps-bookmark-plugin-shadow
          path: build/libs/*-all.jar
          if-no-files-found: error
